<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head')%>
    <%- include('../partials/header')%>
   <body class="listings">
        <h1 class="title">Productos</h1>
        <div class="despliegue">
            <%const j= Math.trunc(productData.length/4) %>
            <%const k= j*2 %>
            <%const l= j*3 %>
                <% for (let i = 0; i < j; i ++) { %>
                    <div class="productos">
                        <img src="/images/products/<%=productData[i].photos%>" alt="imagen de producto">
                        <div class="data">
                            <p>ID: <%= productData[i].id %></p>
                            <p>Prenda: <%= productData[i].productName %></p>
                            <p>Color: <%= productData[i].clothingColor %></p>
                            <p>Talla: <%= productData[i].clothingSize %></p>
                            <p>Precio: <%= productData[i].price %></p>
                            <a href="/admin/modificar-producto/<%=productData[i].id%>" class="action-button edit">Editar</a>
                            <form action="/admin/<%=productData[i].id%>?_method=DELETE" method="POST" style="display: inline-flex">
                                <button type="submit" class="list-button">Eliminar</button>
                            </form>
                        </div>
                    </div>
     
                <% } %>
        </div>
        <div class="despliegue">
                <% for (let i = j ; i < k; i ++) { %>
                    <div class="productos">
                        <img src="/images/products/<%=productData[i].photos%>" alt="imagen de producto">
                        <div class="data">
                            <p>ID: <%= productData[i].id %></p>
                            <p>Prenda: <%= productData[i].productName %></p>
                            <p>Color: <%= productData[i].clothingColor %></p>
                            <p>Talla: <%= productData[i].clothingSize %></p>
                            <p>Precio: <%= productData[i].price %></p>
                            <a href="/admin/modificar-producto/<%=productData[i].id%>" class="action-button edit">Editar</a>
                            <form action="/admin/<%=productData[i].id%>?_method=DELETE" method="POST" style="display: inline-flex">
                                    <button type="submit" class="list-button">Eliminar</button>
                            </form>
                        </div>
                    </div>
     
                <% } %>
        
        </div>
        </div>
        <div class="despliegue">
                <% for (let i = k ; i < l; i ++) { %>
                    <div class="productos">
                        <img src="/images/products/<%=productData[i].photos%>" alt="imagen de producto">
                        <div class="data">
                            <p>ID: <%= productData[i].id %></p>
                            <p>Prenda: <%= productData[i].productName %></p>
                            <p>Color: <%= productData[i].clothingColor %></p>
                            <p>Talla: <%= productData[i].clothingSize %></p>
                            <p>Precio: <%= productData[i].price %></p>
                            <a href="/admin/modificar-producto/<%=productData[i].id%>" class="action-button edit">Editar</a>
                            <form action="/admin/<%=productData[i].id%>?_method=DELETE" method="POST" style="display: inline-flex">
                                    <button type="submit" class="list-button">Eliminar</button>
                            </form>
                        </div>
                    </div>
 
                <% } %>
    
        </div>
        </div>
            <div class="despliegue">
                    <% for (let i = l ; i < productData.length; i ++) { %>
                        <div class="productos">
                            <img src="/images/products/<%=productData[i].photos%>" alt="imagen de producto">
                            <div class="data">
                                <p>ID: <%= productData[i].id %></p>
                                <p>Prenda: <%= productData[i].productName %></p>
                                <p>Color: <%= productData[i].clothingColor %></p>
                                <p>Talla: <%= productData[i].clothingSize %></p>
                                <p>Precio: <%= productData[i].price %></p>
                                <a href="/admin/modificar-producto/<%=productData[i].id%>" class="action-button edit">Editar</a>
                                <form action="/admin/<%=productData[i].id%>?_method=DELETE" method="POST" style="display: inline-flex">
                                            <button type="submit" class="list-button">Eliminar</button>
                                </form>
                            </div>
                        </div>
     
                    <% } %>
        
            </div>
    </body>
</html>
